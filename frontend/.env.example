# Local development leaves this unset — Vite proxies /api to localhost:8000.
# In Vercel, set this to your Render backend URL (Project → Settings → Env Vars):
#   VITE_API_BASE=https://job-scraper-api.onrender.com
VITE_API_BASE=
