# Optional dependency for the LOCAL Cloudflare-bypass scrape engine
# (backend/app/run_cf_scrape.py). NOT installed on Render — the cloud httpx
# scheduler never scrapes engine:cf companies. curl_cffi impersonates a real
# Chrome TLS/JA3 fingerprint, which gets past Cloudflare's bot-fight on
# Jobvite/Phenom sites (Ampere, Synaptics, …) without a browser.
#
#   pip install -r backend/requirements-cf.txt
#   DATABASE_URL="<prod url>" python -m backend.app.run_cf_scrape
#
curl_cffi>=0.15.0
